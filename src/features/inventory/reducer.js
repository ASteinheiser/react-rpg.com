
const initialState = {
  items: [],
  count: 0,
  maxItems: 5
};

const inventoryReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
};

export default inventoryReducer;
