version: 0.2

phases:
  install:
    commands:
      - yarn install
  build:
    commands:
      -  yarn build
  post_build:
    commands:
      - gh-pages -b gh-pages
cache:
  paths:
    - 'node_modules/**/*'
